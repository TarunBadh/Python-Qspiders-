#by default the input fun will store the vale in the form of string format 
#If we want the data in particular format we have to typecast it.
#eval()

a=eval(input("enter a value 1:"))
print(a)
print(type(a))
